<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysUserPostMapper">

	<resultMap type="SysUserPost" id="SysUserPostResult">
		<result property="userId"     column="user_id"      />
		<result property="postId"     column="post_id"      />
		<result property="platformNo" column="platform_no"  />
	</resultMap>

	<delete id="deleteUserPostByUserId" parameterType="Long">
		delete from sys_user_post where user_id=#{userId}
	</delete>
	
	<delete id="deleteUserPostByUserIdWithPlatform">
		delete from sys_user_post where user_id=#{userId}  <if test="platformNo != null and platformNo != ''">
		AND platform_no = #{platformNo}
	</if>
	</delete>
	
	<select id="countUserPostById" resultType="Integer">
	    select count(1) from sys_user_post where post_id=#{postId}  
	</select>
	
	<select id="countUserPostByIdWithPlatform" resultType="Integer">
	    select count(1) from sys_user_post where post_id=#{postId}  <if test="platformNo != null and platformNo != ''">
		AND platform_no = #{platformNo}
	</if>
	</select>
	
	<delete id="deleteUserPost" parameterType="Long">
 		delete from sys_user_post where user_id in
 		<foreach collection="array" item="userId" open="(" separator="," close=")">
 			#{userId}
        </foreach> 
 	</delete>
	
	<delete id="deleteUserPostWithPlatform">
 		delete from sys_user_post where user_id in
 		<foreach collection="ids" item="userId" open="(" separator="," close=")">
 			#{userId}
        </foreach>
		<if test="platformNo != null and platformNo != ''">
			AND platform_no = #{platformNo}
		</if>
 	</delete>
	
	<insert id="batchUserPost">
		insert into sys_user_post(user_id, post_id, platform_no) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.userId},#{item.postId},#{item.platformNo})
		</foreach>
	</insert>
	
</mapper> 